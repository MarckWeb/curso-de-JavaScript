const autenticado = true;

// si se cumple si ejectua uno y si es false ejectua el otor
if(autenticado){
    console.log( 'si puedes pasar')
} else {
    console.log('no puedes pasar')
}

// operador ternario

console.log(autenticado) ? 'si puedes pasar' : 'no puedes pasar';
